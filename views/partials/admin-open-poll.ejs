<h3 class="center">Your Poll Results</h3><br>
<table class="centered">
  <thead>
    <tr>
      <th>Option</th>
      <th>Count</th>
    </tr>
  </thead>
  <tbody id="choices">
    <% for(var i=0; i < poll.options.length; i++){ %>
      <tr>
        <% if(poll.options[i] != ""){ %>
          <td><h5><%=poll.options[i]%></h5></td>
          <td id="<%=poll.options[i].toUpperCase()%>-votes"> <%= votes[poll.options[i].toUpperCase()]%> </td>
        <% } %>
      </tr>
    <% } %>
  </tbody>
</table>
